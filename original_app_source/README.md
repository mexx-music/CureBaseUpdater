# original_app_source

Drop a copy of the original Android app source here. Use this folder to store a pristine copy of the app code you can compare against or restore from.

Usage:
- Copy the complete app source tree here (via Finder or `cp -r /path/to/app_source/* original_app_source/`).
- Recommended: keep this folder separate from active development to avoid accidental edits.

(Created by CureBaseUpdater workspace helper)
